export interface Criteres {
  budgetMin: number;
  budgetMax: number;
  nombreSieges: Array<number>; // Tous les nombres de sièges désirés
  typeVehicule: Array<string>; // Tous les types de véhiculess désirés
}
